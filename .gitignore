# Local server state (contains API keys typed into the panel — never commit)
core/state.json
core/activity.log
core/backups/

# Cloned/tracked repos (machine-local)
repo/*
!repo/.gitkeep

# Danger Zone zip backups — kept out of repo/ on purpose (repo/ is only for cloned skills/MCP)
backups/*
!backups/.gitkeep

# Go build artifacts
ai-toolkit.exe

# Go binaries
*.exe
*.exe~
